defaults:
  - default
  - _self_

model: "gpt-5-nano:openai"
reasoning_effort: "minimal"
identity_prompt_path: "identity"
identity_extraction_prompt_path: "identity_extraction"
explicit_cot: true

num_tests: 3

only_couples: true
answer_progress_minimum: 0.7
